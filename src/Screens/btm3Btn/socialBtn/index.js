import React from 'react';
import {Text, SafeAreaView} from 'react-native';

const SocialBtn = () => {
     return(
          <SafeAreaView>
               <Text>
                    this is social Btn
               </Text>
          </SafeAreaView>
     );
};

export default SocialBtn;