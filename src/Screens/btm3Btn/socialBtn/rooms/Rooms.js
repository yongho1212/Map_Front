import React from 'react'
import {Text, SafeAreaView} from 'react-native';

const Rooms = () => {
     return(
          <SafeAreaView>
               <Text>
                    Rooms
               </Text>
          </SafeAreaView>
     );
};

export default Rooms;