import React from 'react';
import {Text, SafeAreaView, View} from 'react-native';

const HostingBtn = () => {
     return(
          <SafeAreaView>
               <Text>
                    this is Hosting button
               </Text>
          </SafeAreaView>
     );
};

export default HostingBtn;